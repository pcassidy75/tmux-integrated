{
  "name": "tmux-integrated",
  "displayName": "tmux-integrated",
  "description": "Seamless tmux integration for VS Code terminals — persistent sessions, code-command support, and Copilot compatibility",
  "version": "0.3.1",
  "publisher": "pcassidy75",
  "license": "GPL-3.0-only",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/pcassidy75/tmux-integrated"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "tmux",
    "terminal",
    "persistent",
    "session"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "extensionKind": [
    "workspace"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "terminal": {
      "profiles": [
        {
          "id": "tmux-integrated.terminal",
          "title": "tmux-integrated"
        }
      ]
    },
    "commands": [
      {
        "command": "tmux-integrated.newTerminal",
        "title": "New tmux Terminal",
        "category": "tmux"
      },
      {
        "command": "tmux-integrated.attachWindow",
        "title": "Attach to tmux Window",
        "category": "tmux"
      },
      {
        "command": "tmux-integrated.renameTerminal",
        "title": "Rename tmux Terminal",
        "category": "tmux",
        "icon": "$(edit)"
      }
    ],
    "menus": {
      "terminal/context": [
        {
          "command": "tmux-integrated.renameTerminal",
          "group": "1_rename@1"
        }
      ]
    },
    "configuration": {
      "title": "tmux-integrated",
      "properties": {
        "tmux-integrated.sessionName": {
          "type": "string",
          "default": "",
          "description": "Override the tmux session name. Defaults to the workspace folder name."
        },
        "tmux-integrated.shell": {
          "type": "string",
          "default": "",
          "description": "Shell to use inside tmux panes. Defaults to $SHELL or /bin/bash."
        },
        "tmux-integrated.cwd": {
          "type": "string",
          "default": "",
          "description": "Starting directory for new tmux terminals. Supports ${workspaceFolder}. Falls back to terminal.integrated.cwd if unset."
        },
        "tmux-integrated.autoConnect": {
          "type": "boolean",
          "default": true,
          "description": "Automatically connect to existing tmux sessions associated with the workspace when VS Code opens."
        },
        "tmux-integrated.showAutomaticRename": {
          "type": "boolean",
          "default": false,
          "description": "Show tmux's automatic window name in the terminal tab so the title tracks the foreground process (like a regular VS Code terminal). When disabled (the default), managed windows are pinned to stable 'tmux:<index>'-style labels. Explicit renames (the built-in Rename action or 'Rename tmux Terminal') always pin the name for that window; with this setting enabled, submitting an empty name in 'Rename tmux Terminal' returns the window to automatic naming. Takes effect for terminals created or adopted after the change."
        },
        "tmux-integrated.closeStrayShellsOnActivation": {
          "type": "boolean",
          "default": true,
          "description": "When terminal.integrated.defaultProfile.<os> is set to 'tmux-integrated', automatically dispose any non-tmux shell terminal that the workbench spawned before this extension could register its profile provider. The race is editor-side (see README troubleshooting); the cleanup is purely cosmetic and preserves any tmux-backed tabs."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "eslint src",
    "test": "npm run compile && node --test test/*.test.js",
    "changelog": "node scripts/update-changelog.js",
    "version": "npm run changelog && git add CHANGELOG.md",
    "release:beta": "node scripts/release.js beta",
    "release:stable": "node scripts/release.js stable",
    "release": "node scripts/release.js stable"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^3.9.1",
    "eslint": "^9.39.4",
    "ovsx": "^0.10.10",
    "typescript": "^5.1.0",
    "typescript-eslint": "^8.60.0"
  },
  "dependencies": {
    "node-pty": "1.2.0-beta.13"
  }
}
